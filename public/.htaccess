<IfModule mod_rewrite.c>
  RewriteEngine On
  RewriteBase /
  # SPA owns /blog and /blog/* (headless WordPress; API at separate wp-json URL)
  RewriteRule ^index\.html$ - [L]
  RewriteCond %{REQUEST_FILENAME} !-f
  RewriteCond %{REQUEST_FILENAME} !-d
  RewriteRule . /index.html [L]
</IfModule>
